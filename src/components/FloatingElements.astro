---
---
<div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div class="floating-element absolute top-20 left-[10%] w-8 h-8 text-3xl animate-float-slow">🦮</div>
    <div class="floating-element absolute top-40 right-[15%] w-8 h-8 text-3xl animate-float-medium">🐱</div>
    <div class="floating-element absolute bottom-20 left-[20%] w-8 h-8 text-3xl animate-float-fast">🦴</div>
</div>

<style>
    @keyframes float-slow {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
    }
    @keyframes float-medium {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-15px); }
    }
    @keyframes float-fast {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
    .animate-float-slow {
        animation: float-slow 6s ease-in-out infinite;
    }
    .animate-float-medium {
        animation: float-medium 5s ease-in-out infinite;
    }
    .animate-float-fast {
        animation: float-fast 4s ease-in-out infinite;
    }
</style> 
---
---
<div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div class="floating-element absolute top-[15%] left-[10%] w-8 h-8 text-3xl animate-float-slow opacity-40">🐕</div>
    <div class="floating-element absolute top-[30%] right-[15%] w-8 h-8 text-3xl animate-float-medium opacity-40">🐱</div>
    <div class="floating-element absolute bottom-[20%] left-[20%] w-8 h-8 text-3xl animate-float-fast opacity-40">🐾</div>
    <div class="floating-element absolute bottom-[30%] right-[25%] w-8 h-8 text-3xl animate-float-slow opacity-40">🦴</div>
</div>

<style>
    @keyframes float-slow {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-20px) rotate(5deg); }
    }
    @keyframes float-medium {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-15px) rotate(-5deg); }
    }
    @keyframes float-fast {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-10px) rotate(3deg); }
    }
    .animate-float-slow {
        animation: float-slow 6s ease-in-out infinite;
    }
    .animate-float-medium {
        animation: float-medium 5s ease-in-out infinite;
    }
    .animate-float-fast {
        animation: float-fast 4s ease-in-out infinite;
    }
</style> 